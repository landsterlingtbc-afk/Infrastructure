# دليل الإعداد والنشر — منصة TBC (النسخة الإنتاجية)

هذا الدليل خطوات مرقّمة بسيطة — لا حاجة لخبرة برمجية لتنفيذها. كل ما تحتاجه:
حساب بريد إلكتروني لإنشاء حسابي Supabase و Netlify (مجانيان).

## المرحلة ١ — إنشاء قاعدة البيانات (Supabase)

1. اذهب إلى https://supabase.com وأنشئ حسابًا مجانيًا (تسجيل عبر GitHub أو البريد).
2. اضغط "New Project" — اختر اسمًا (مثلاً `tbc-platform`) وكلمة مرور قوية لقاعدة البيانات (احفظها في مكان آمن، ستحتاجها نادرًا).
3. بعد إنشاء المشروع (يستغرق دقيقة تقريبًا)، من القائمة الجانبية اذهب إلى **SQL Editor**.
4. افتح ملف `db/schema.sql` المرفق معي، انسخ محتواه بالكامل، الصقه في محرر SQL، واضغط **Run**.
   - يجب أن تظهر رسالة نجاح بدون أخطاء. هذا ينشئ كل الجداول فارغة (لا بيانات وهمية إطلاقًا).
5. من القائمة الجانبية اذهب إلى **Project Settings → API**. ستحتاج نسخ قيمتين وإرسالهما لي أو حفظهما لخطوة لاحقة:
   - `Project URL` (يشبه `https://xxxxx.supabase.co`)
   - `service_role` **secret key** (تحت "Project API keys" — ⚠️ هذا المفتاح سرّي جدًا، لا تضعه إلا في إعدادات Netlify لاحقًا، ولا تشاركه في أي مكان آخر)
   - `anon public` key أيضًا (هذا يمكن أن يكون علنيًا، يُستخدم في الواجهة الأمامية)

## المرحلة ٢ — إنشاء أول حساب إدارة

1. من قائمة Supabase الجانبية اذهب إلى **Authentication → Users → Add user**.
2. أدخل بريدك الإلكتروني وكلمة مرور، واضغط إنشاء.
3. اذهب إلى **SQL Editor** ونفّذ الأمر التالي (بعد استبدال البريد بالبريد الذي استخدمته):
   ```sql
   update profiles set role = 'admin', display_name = 'الإدارة'
   where id = (select id from auth.users where email = 'بريدك@هنا.com');
   ```
   هذا يمنح هذا الحساب صلاحيات الإدارة الكاملة.

## المرحلة ٣ — نشر الموقع على Netlify

1. اذهب إلى https://app.netlify.com وأنشئ حسابًا (الأسهل: عبر GitHub مباشرة).
2. **الطريقة الموصى بها (تحديث تلقائي):**
   - أنشئ مستودعًا فارغًا في GitHub، وسأدفع لك الكود إليه (أو أرسل لك تعليمات لرفعه بنفسك إن لم يكن لديّ وصول لحسابك).
   - من Netlify: **Add new site → Import an existing project → GitHub** → اختر المستودع.
   - اترك إعدادات البناء كما هي (Netlify يقرأ `netlify.toml` تلقائيًا) واضغط **Deploy**.
3. **أو الطريقة اليدوية (كما اعتدتم):**
   - نفّذ `npm install` على الملف محليًا، ثم اسحب مجلد المشروع كاملاً إلى صفحة Netlify Drop (https://app.netlify.com/drop).
4. بعد أول نشر، اذهب إلى **Site settings → Environment variables** وأضف:
   | المتغير | القيمة |
   |---|---|
   | `SUPABASE_URL` | Project URL من المرحلة ١ |
   | `SUPABASE_SERVICE_ROLE_KEY` | service_role secret key من المرحلة ١ |
   | `SUPABASE_ANON_KEY` | anon public key من المرحلة ١ |
5. من تبويب **Deploys** اضغط **Trigger deploy → Deploy site** مرة أخرى حتى تُفعَّل متغيرات البيئة الجديدة.

## المرحلة ٤ — أول تسجيل دخول

افتح رابط موقعك على Netlify (يشبه `your-site.netlify.app`)، سجّل الدخول بالبريد وكلمة المرور اللذين أنشأتهما في المرحلة ٢. يجب أن تدخل بصلاحيات الإدارة الكاملة.

## ملاحظة مهمة

هذه المرحلة الأولى من النظام الإنتاجي تغطي: المصادقة الحقيقية، المدارس، البلاغات، والحصورات (رفع وعرض). صفحات الزيارات والتقارير والصفحات الثانوية الأخرى قيد الإنجاز في المرحلة التالية — سأخبرك أولًا بأول لحظة تحتاج فيها فعل شيء، وإلا فواصل العمل من جهتي.
